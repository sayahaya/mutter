<%= form_with model: @mutter, local: true do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: "mutterを投稿..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>